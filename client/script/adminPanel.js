// Function to open modal for creating or editing workout
function openWorkoutModal(mode) {
    // Implement logic to open modal
    console.log('Open workout modal for ' + mode);
}

// Function to open modal for creating or editing song
function openSongModal(mode) {
    // Implement logic to open modal
    console.log('Open song modal for ' + mode);
}
